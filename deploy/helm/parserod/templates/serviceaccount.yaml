{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "parserod.serviceAccountName" . }}
  labels:
    {{- include "parserod.labels" . | nindent 4 }}
{{- end }}
